import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'
import Header from '../../../../components/ProfileHeader'

export default function AddVisaMasterCard() {
	return (
		<SafeAreaView>
			<Header back name={'Add Visa/Master Card'} />
		</SafeAreaView>
	)
}