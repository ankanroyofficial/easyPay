import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'
import Header from '../../../../components/Header'

export default function AddMaestroCard() {
	return (
		<SafeAreaView>
			<Header back name={'Add Maestro Card'} />
		</SafeAreaView>
	)
}